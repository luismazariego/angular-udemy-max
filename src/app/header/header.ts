import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  standalone: false, // no need to put this but I will do it for the sake of learning
  templateUrl: './header.html',
  styleUrl: './header.css'
})
export class HeaderComponent { }
